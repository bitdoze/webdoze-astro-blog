---
export type Heading = {
  subheadings: Heading[];
  depth: number;
  slug: string;
  text: string;
};

interface Props {
  heading: Heading;
}
const { heading } = Astro.props;
---

<li class="hover:text-blue-500 dark:hover:text-blue-400">
  <a href={"#" + heading.slug} class="text-gray-800 dark:text-gray-200">
    {heading.text}
  </a>
  {
    heading.subheadings.length > 0 && (
      <ul class="pl-5">
        {heading.subheadings.map((subheading) => (
          <Astro.self heading={subheading} />
        ))}
      </ul>
    )
  }
</li>
