---
export interface Props {
  url: string;
  title: string;
  description: string;
  icon: string;
}

const { url, title, description, icon } = Astro.props;
---

<a href={url} class="block group">
  <div
    class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-md transition-all duration-300 transform group-hover:shadow-xl group-hover:-translate-y-1"
  >
    <div class="flex items-center mb-4">
      <div class="bg-indigo-100 dark:bg-indigo-900/40 rounded-full p-3 mr-4">
        <i class={`fas ${icon} text-2xl text-indigo-600 dark:text-indigo-400`}></i>
      </div>
      <h3
        class="text-xl font-semibold text-gray-800 dark:text-gray-200 group-hover:text-indigo-600 dark:group-hover:text-indigo-400 transition-colors duration-300"
      >
        {title}
      </h3>
    </div>
    <p class="text-gray-600 dark:text-gray-400">
      {description}
    </p>
  </div>
</a>
