---
interface Props {
  title: string;
  subtitle: string;
  loadingText: string;
}

const { title, subtitle, loadingText } = Astro.props;
---

<div id="output" class="mt-8 hidden">
  <div class="relative">
    <div class="absolute -inset-2 rounded-[32px] bg-gray-300/50 dark:bg-gray-700/50"></div>
    <div class="relative rounded-3xl bg-white dark:bg-gray-800 p-6 shadow-sm">
      <h2 class="text-3xl font-bold mb-6 text-center text-indigo-700 dark:text-indigo-400">
        {title}
      </h2>
      <p class="text-gray-600 dark:text-gray-400 mb-4 text-center">
        {subtitle}
      </p>

      <div id="loadingIndicator" class="hidden">
        <p class="text-indigo-600 dark:text-indigo-400 text-center animate-pulse">
          {loadingText}
        </p>
      </div>

      <div id="errorMessage" class="hidden text-red-500 text-center mb-4"></div>

      <div class="max-h-80 overflow-y-auto pr-2 space-y-3" id="resultList">
        <!-- Results will be inserted here dynamically -->
      </div>
    </div>
  </div>
</div>

<style>
  /* Custom scrollbar styles */
  .max-h-80::-webkit-scrollbar {
    width: 8px;
  }
  .max-h-80::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 10px;
  }
  @media (prefers-color-scheme: dark) {
    .max-h-80::-webkit-scrollbar-track {
      background: #2d3748;
    }
  }
  .max-h-80::-webkit-scrollbar-thumb {
    background: #888;
    border-radius: 10px;
  }
  @media (prefers-color-scheme: dark) {
    .max-h-80::-webkit-scrollbar-thumb {
      background: #4a5568;
    }
  }
  .max-h-80::-webkit-scrollbar-thumb:hover {
    background: #555;
  }
  @media (prefers-color-scheme: dark) {
    .max-h-80::-webkit-scrollbar-thumb:hover {
      background: #718096;
    }
  }
</style>
